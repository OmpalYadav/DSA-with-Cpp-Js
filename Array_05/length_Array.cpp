#include <bits/stdc++.h>
using namespace std;

int main() {
    int arr[] = {1, 2, 3, 4, 5};

    // Finding the length of the array
    int n = sizeof(arr) / sizeof(arr[0]); //we can aslo use here sizeof(int)

    cout << n;
    return 0;
}